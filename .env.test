# Test Environment Variables
# These are mock values for testing purposes
# Replace with real test credentials for actual testing

# Supabase Test Configuration
NEXT_PUBLIC_SUPABASE_URL=https://test-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=test-anon-key-replace-with-real-key
SUPABASE_SERVICE_ROLE=test-service-role-key-replace-with-real-key

# Stripe Test Configuration
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_mock_stripe_key_replace_with_real_key
STRIPE_SECRET_KEY=sk_test_mock_stripe_key_replace_with_real_key
STRIPE_WEBHOOK_SECRET=whsec_test_webhook_secret_replace_with_real_key

# Email Configuration (for testing)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=test@example.com
SMTP_PASS=test_password

# Application Configuration
NODE_ENV=test
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Security
CRON_SECRET_TOKEN=test_cron_secret_token

# Database
DATABASE_URL=postgresql://test:test@localhost:5432/pactify_test

# Additional Test Configuration
TEST_MODE=true
MOCK_PAYMENTS=true
MOCK_EMAIL=true